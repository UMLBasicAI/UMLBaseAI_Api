version: 2.1

executors:
  dotnet-executor:
    docker:
      - image: mcr.microsoft.com/dotnet/sdk:8.0
    working_directory: ~/project

jobs:
  build:
    executor: dotnet-executor
    steps:
      - checkout

      - run:
          name: Restore dependencies
          command: dotnet restore Src/Entry/Entry.csproj

      - run:
          name: Build entry project
          command: dotnet build Src/Entry/Entry.csproj --configuration Release --no-restore

      - run:
          name: Run tests (nếu có)
          command: echo "No tests defined - skip or add here"

      - run:
          name: Publish app
          command: dotnet publish Src/Entry/Entry.csproj -c Release -o ./publish --self-contained false

      - persist_to_workspace:
          root: .
          paths:
            - publish

  deploy:
    executor: dotnet-executor
    steps:
      - attach_workspace:
          at: ~/project

      - add_ssh_keys:
          fingerprints:
            - "SHA256:l773R58oKSwz7pggORkLHL2XxUko5EMWHOMuu2AvS/8" 

      - run:
          name: Install SCP
          command: | 
              apt-get update && apt-get install -y openssh-client

      - run:
          name: Deploy to Azure VPS
          command: |
            ssh -o StrictHostKeyChecking=no adminplantuml@20.2.88.32 \<< 'EOF'
              # Check if dotnet is installed
              if ! command -v dotnet &> /dev/null; then
                echo ".NET not found. Installing .NET SDK and Runtime..."
                wget https://packages.microsoft.com/config/ubuntu/22.04/packages-microsoft-prod.deb -O packages-microsoft-prod.deb
                sudo dpkg -i packages-microsoft-prod.deb
                rm packages-microsoft-prod.deb
                sudo apt-get update
                sudo apt-get install -y dotnet-sdk-8.0 aspnetcore-runtime-8.0
              else
                echo ".NET is already installed:"
                dotnet --info
              fi

              # Create target directory
              mkdir -p /home/adminplantuml/apps/umlbasicaiapi/production
            EOF

            scp -o StrictHostKeyChecking=no -r ./publish/* adminplantuml@20.2.88.32:/home/adminplantuml/apps/umlbasicaiapi/production
            ssh -o StrictHostKeyChecking=no adminplantuml@20.2.88.32 "sudo systemctl daemon-reload"
            ssh -o StrictHostKeyChecking=no adminplantuml@20.2.88.32 "sudo systemctl restart umlbasicaiapi"
            ssh -o StrictHostKeyChecking=no adminplantuml@20.2.88.32 "sudo systemctl status umlbasicaiapi --no-pager"

workflows:
  build-deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
